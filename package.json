{
  "name": "real-estate-exam",
  "version": "1.0.0",
  "description": "Real Estate Exam Practice App",
  "scripts": {
    "build": "cp index.html www/index.html && mkdir -p www/fonts && cp fonts/*.woff2 www/fonts/",
    "build:ios": "npm run build && npx cap copy ios",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open ios"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nilehanov/real-estate-exam.git"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor/core": "^8.3.0",
    "@capacitor/ios": "^8.3.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.0"
  }
}
